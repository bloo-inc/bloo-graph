let AuthenticationError;_00a‍.x([["type",()=>type],["resolvers",()=>resolvers]]);_00a‍.w("apollo-server",[["AuthenticationError",["AuthenticationError"],function(v){AuthenticationError=v}]]);

const type = (`
  type User {
    uuid : String!
    username : String!
    email : String!
    name : String!
  }

  extend type Query {
    self : User
  }
`)

const resolvers = {
  Query : {
    self: (root, {}, {user, log, db, err}) => {
      if (!user) { throw new AuthenticationError("Unauthorized") }
      return user;
    }
  }
}


